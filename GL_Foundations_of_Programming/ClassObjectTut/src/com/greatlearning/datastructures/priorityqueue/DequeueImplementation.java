package com.greatlearning.datastructures.priorityqueue;

public class DequeueImplementation {
    /*
     * java.utils package
     * interface which impelements Queue interface and provide implementation for a doubley ended queue
     * DQ -> Ordering is maintained
     * DQ -> Hence, random access and manipulation is possible
     * Can allow duplicates and null
     * SuperInterfaces: Collections<E>, Iterable<E>, Queue<E>
     * Subinterface: BlockingDequeue<E>
     * Concerete Implementations:
     *      ArrayDequeue, ConcurrentLinkedDequeue, LinkedBlockingDeque, LinkedList
     * Methods:
     *      addFirst()->throws
     *      offerFirst()-> silent
     *      addLast()->  similar to add() for Priority Queue
     *      offerLast()-> similar to offer() for Priority Queue
     *      removeFirst()-> similar to remove() for Priority Queue
     *      pollFirst()-> similar to poll() for Priority Queue
     *      removeLast() -> throws
     *      pollLast()-> silent
     *      getFirst()-> similar to element() for Priority Queue
     *      peekFirst() ->silent
     *      getLast() -> throws
     *      PeekLast() -> silent
     * 
     *      element()
     *      contains(Object o) -> returns true if DQ contains object
     *      descendingIterator() _> returns an iterator over the elements in a reverse sequence
     */
    
}
