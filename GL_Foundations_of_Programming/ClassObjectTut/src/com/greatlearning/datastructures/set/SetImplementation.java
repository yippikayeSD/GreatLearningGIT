package com.greatlearning.datastructures.set;

public class SetImplementation {

    /*
     * A collection that doesnt not allow duplicate elements
     * Imitates mathematical sets in programming environment
     * An interface which extends collection
     * Maintains ordering, hence enables random access and manipulation
     * java.util
     * Subinterfaces: NavigableSet<E>, SortedSet<E>
     * Concrete Implementations: AbstractSet, CopyOnWriteArraySet, EnumSet, HashSet, LinkedHashSet, TreeSet
     * Methods:
     *      add(E e) -> adds element if not already present, returns false if unable to add
     *      addAll(Collection<? extends E> c) -> adds all elements in collection
     *      contains(Object o) -> returns true if set contains element
     *      containsAll(Collection<? > c) -> returns true if entire collection is prsent in the set
     *      equals(Object o) -> Compares the specified object with this set for equality.
     *      remove()
     *      iterator()
     *      
     * 
     */
    
}
