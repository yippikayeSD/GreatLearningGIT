package com.greatlearning.datastructures.set;

public class LinkedHashSetimplementation {
    /*
     * Sepcial type of has set -> ordered in nature
     * Makes ouse of Doubley Linked List
     * implements List interface, hence maintains order of insertion
     * Since impelents HashSet, cannot contain duplicates
     * Less efficient that HashSet since needs to maintain order
     * 
     * implements: Serializable, Cloneable, Iterable<E>, Collection<E>,Set<E>
     * Extends HashSet<E>
     * =>Not Synchronised
     * Constructor:
     *      LinkedHashSet() -> empty LHS, default capacity 16, load factor 0.75
     *      LinkedHashSet(Collection< ? extends E> c) -> constructs LHS with same elements as specified collection
     *      LinkedHashSet(int initialCapacity) -> Constructs empty LHS with specified initialCapacity
     *      LinkedHashSet(int nitialCapacity, float loadFactor) -> constructs empty LHS with specified nitialCapacity and loadFactor
     * Methods:
     *      Inherited From HashSet:
     *          add,clear,clone,contains,isEmpty,iterator, remove, size
     *      Inherited From AbstractSet:
     *          equals, hashCode, removeAll
     *      Inherited From AbstractCollection:
     *          addAll, containsAll, retainAll, toArray, toString
     *      k
     * 
     */
    
}
